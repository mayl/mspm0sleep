[package]
name = "repro-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
rusb = "0.9"
plotters = { version = "0.3", default-features = false, features = ["bitmap_backend", "bitmap_encoder", "svg_backend", "line_series"] }
